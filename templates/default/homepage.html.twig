{% extends 'base.html.twig' %}

{% block title %}ShowDB{% endblock %}

{% block body %}
<h1>Bienvenue sur ShowDB !</h1>
<h3>Le site où vous pouvez enregistrer vos séries préférées...</h3>

<h2>Vos dernières séries :</h2>
    {% if shows is not empty %}
        {% for show in shows %}
            <a href="{{ path('show_detail', { 'id': show.id }) }}" ><li>{{ show.title }}</li></a>
        {% endfor %}
    {% else %}
    <p>Vous n'avez pas encore enregistré de série...</p>
    {% endif %}
<h2>Ajouter une série :</h2>

{% endblock %}
